import React from 'react';
import './App.css';

const user = {
  firstName : 'Jokanola',
  lastNAme : 'Adedipupo'
}

function User(user) {
  return `user.firstName + '' + user.lastName` 
}

function App(user) {
  return (
    <div>
     
     
    </div>
  );
}

export default App;
